/**
 * Canonical origin for the site.
 *
 * Update this to the production domain before the first deploy. Astro uses it
 * for canonical URLs, Open Graph tags and sitemap.xml, so it must be an
 * absolute origin with no trailing slash.
 */
export const SITE_URL = 'https://frances-sun-portfolio.vercel.app';
